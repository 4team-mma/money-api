# 定義特徵:把所有字串比對的邏輯集中在這裡，未來要增加「保險」或「股票」意圖，只需在這裡加 Pattern。
import re


# 意圖偵測專用 Pattern
INTENT_PATTERNS = {
    "number": re.compile(r'\d+'),
    "money_unit": re.compile(r'(元|塊|萬|千|張|筆)'),
    
    # 記帳動作
    "record_action": re.compile(r'(花|買|記帳|支出|消費|吃了|花了|付|中獎|收入|賺|薪水|收|匯|轉帳|轉給|轉到|轉出|轉入|存|領|轉)'),
    
    # 查詢與分析觸發
    "query_trigger": re.compile(r'(多少|剩|總共|統計|分析|餘額|明細|占比|排行|有沒有|吃了沒|買了沒|過|紀錄|查詢|找一下|答案|結果|多少錢|算了沒|錢|資產|銀行|存款|台新|錢包|可以|夠嗎|夠不夠|評估|能不能|預算查詢)'),
    
    # 理財顧問觸發
    "advisor_trigger": re.compile(r'(健檢|建議|理財顧問|檢視|診斷|投資建議|消費基準|物價|漲價|通膨|cpi|貴|嚴重|指標)'),
    
    # 系統知識觸發 (這部分最容易誤判，我們在規則裡限縮)
    "knowledge_trigger": re.compile(r'(怎麼用|設定|成就|解鎖|規則|什麼是|卡牌|等級|怎麼|如何|手冊)'),
    
    # 懷疑與提醒
    "doubt_trigger": re.compile(r'(為什麼|怎算的|算錯|不對|為啥|不是吧|提醒|繳費|行事曆|忘記|預算)'),
    
    #  新增：社交與招呼語 (用於救援被誤判的 CHAT)
    "social_greeting": re.compile(r'(你好|早安|午安|晚安|早上好|謝謝|妙妙|喵|回來了|哈囉|hi|hello|再見|拜拜|辛苦了)')
}