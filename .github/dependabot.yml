version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"

    versioning-strategy: lockfile-only
    open-pull-requests-limit: 5

    groups:
      frontend-security:
        applies-to: security-updates # 關鍵是這一行
        patterns:
          - "*"
